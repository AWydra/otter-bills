import { StyleInterface } from 'interfaces';

const styles: StyleInterface = {
  heading: {
    paddingTop: 3,
    paddingX: 2,
    paddingBottom: 2,
  },
  subtitle: {
    paddingTop: 1,
  },
};

export default styles;
