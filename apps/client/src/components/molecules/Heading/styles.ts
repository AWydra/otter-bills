import type { StyleInterface } from 'interfaces';

const styles: StyleInterface = {
  heading: {
    paddingY: 3,
    paddingX: 2,
  },
  subtitle: {
    paddingTop: 1,
    paddingBottom: 2,
  },
};

export default styles;
